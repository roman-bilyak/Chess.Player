<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:vm="clr-namespace:Chess.Player.MAUI.ViewModels"
             x:Class="Chess.Player.MAUI.Pages.AboutPage"
             x:DataType="vm:AboutViewModel"
             Title="About">
    <ScrollView Padding="10" Margin="10">
        <StackLayout Spacing="10">
            <Frame HeightRequest="70"
                   WidthRequest="70"
                   CornerRadius="35">
                <Image Source="logo.svg" 
                       Aspect="AspectFit"
                       VerticalOptions="Center"
                       HorizontalOptions="Center" 
                       HeightRequest="70" 
                       WidthRequest="70" 
                       Margin="0,20" BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}">
                    <Image.Behaviors>
                        <mct:IconTintColorBehavior TintColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}" />
                    </Image.Behaviors>
                </Image>
            </Frame>
            
            <Label Text="Chess Player" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" />
            <Label Text="{Binding Version}" HorizontalOptions="Center" FontSize="10" TextColor="Gray" />

            <Label HorizontalOptions="Start">The "Chess Player" is a mobile app designed by a Ukrainian developer with the aim of popularizing chess in the world. Deep dive into the past performances of players, unraveling their strategies, successes, and challenges in various chess events.</Label>

            <Label HorizontalOptions="Start">For inquiries, contact us at:</Label>

            <Label Text="chess.player@gmail.com"
                       Margin="0,10"
                       HorizontalOptions="Center"
                       TextDecorations="Underline">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding SendEmailCommand}" />
                </Label.GestureRecognizers>
            </Label>

            <Label HorizontalOptions="Start" FontAttributes="Bold">The application is free, but if you've enjoyed it and want to show your support, feel free to buy me a coffee! Positive feedback motivates, and suggestions enhance the app.</Label>

            <Label HorizontalOptions="Start">100% of all proceeds go to support of Ukraine!</Label>

            <Button Text="Buy me a coffee"
                    Command="{Binding BuyMeACoffeeCommand}"
                    VerticalOptions="EndAndExpand"
                    HorizontalOptions="Center"
                    BackgroundColor="#FFD700"
                    TextColor="Black"
                    CornerRadius="10"
                    Padding="15,5"
                    Margin="0,20" />
        </StackLayout>
    </ScrollView>
</ContentPage>